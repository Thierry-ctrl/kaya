# These are PUBLIC build-time settings, not server secrets.
NODE_ENV=production
BASE_PATH=/
VITE_CLERK_PUBLISHABLE_KEY=REPLACE_WITH_EXTERNAL_CLERK_PRODUCTION_PUBLISHABLE_KEY
# Leave unset for an external Clerk instance using its own auth domain.
VITE_CLERK_PROXY_URL=